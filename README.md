# Akash — Portfolio

This is a Vite + React + Tailwind CSS personal portfolio template.

Quick start:

```bash
cd portfolio-site
npm install
npm run dev
```

Notes:
- Add your resume at `public/resume.pdf` for the Download Resume button to work.
- Update links in the components and `src/data/projects.js` to point to your real repos and profiles.
